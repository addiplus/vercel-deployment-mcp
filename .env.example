# Required: Vercel access token (create in Vercel account settings).
# This value is read from the environment only. It is never written to logs,
# error messages, or tool responses. Never commit a real value.
VERCEL_TOKEN=
# Optional: scope all requests to a team.
VERCEL_TEAM_ID=
